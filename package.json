{
  "name": "quiz-api",
  "version": "1.0.0",
  "description": "JSON server for quiz application",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
